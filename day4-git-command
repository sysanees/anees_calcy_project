  500  mkdir project
  501  rm -rf project
  502  mkdir project
  503  cd project
    #touch command is used to create blank file
  504  touch DS_Store
  505  touch calcy.py
  506  touch auth
  507  touch secret
  508  ls
    #we are going to create global variables you can change the name and email id to your name and email id 
  509  git config --global user.name "gopal"
  510  git config --global user.email "gopal1409@gmail.com"
  511  git config --global list
  512  git config --global --list
#inside the folder if i run git status this will show that git is not managing the code
    514  git status
    #git init will create a .git folder which like an versioing database for git 
518  git init
  519  ls -la
  520  ls -la .git
  521  git status
    #if i delete the git folder i ll loose all the versioing information
  522  rm -rf .git
  523  git status
    #lets re instialzied it 
  524  git init
    ################################
 527  cat calcy.py
#we are adding an line in my calcy app
  528  echo "we are starting with an calcy program" > calcy.py
  529  cat calcy.py
  530  git status
#once i added the line we need to move this file to an staging area for commit git add command will move this file to an staging area
  531  git add calcy.py
  532  git status
#finally git commit -m message always give an menaing message so that you now what changes you have done
  533  git commit -m "started calcy program"
  534  git status
#this will show the first commit 
  535  git log

#we will add another line 
  540  echo "we are starting addion program" >> calcy.py
#now git status will show that calcy file is modified as it is comparing the file with your previous commit
  541  git status
#git diff will show what changes is there 
  542  git diff
#lets add it again 
  543  git add calcy.py
  544  git status
#do a commit
  545  git commit -m "added addtion program"
#finally git log will show there is two commit 
  546  git log
#lets generate the ssh key after running this command just press enter enter and enter this will generate the public key
 550  ssh-keygen -t rsa
##to check the public key cat and give the path
  551  cat /c/Users/gopal/.ssh/id_rsa.pub
#lets push an new change
 560  echo "we are ending the calcy project" >> calcy.py
  561  git status
  562  git add calcy.py
  563  git commit -m "ended calcy program"
  564  git push origin main
